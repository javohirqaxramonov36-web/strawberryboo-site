# Optional Telegram bot upgrade
The public site uses only the configured channel URL. A future bot may answer opt-in users, but it must remain in `external-services/telegram-reminder-bot`, use environment variables, keep consent and opt-out records in secure storage, and never collect contacts through the static site.
