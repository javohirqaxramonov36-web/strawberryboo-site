import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://javohirqaxramonov36-web.github.io',
  base: '/strawberryboo-site',
  // 8GB RAM uchun minimal config
});
