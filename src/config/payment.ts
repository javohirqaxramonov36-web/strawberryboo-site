// Unified payment link (inPAY.uz aggregator).
//
// inPAY shows Payme / Click / bank card options on its own page, so the site
// only needs ONE link — the user picks the method on inPAY's page.
//
// TODO: replace '#' with the real inPAY payment URL once it is created.
// This is the ONLY value to change; everything else reads from here.
export const PAYMENT_URL = '#';
